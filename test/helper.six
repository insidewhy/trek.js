import expect from "chai"
import {parse: p} from "../trek"

export var spacing = p.plus(" "),
           parse   = (grammar, src) => p.parse(spacing, grammar, src),
           p       = p,
           expect  = expect

